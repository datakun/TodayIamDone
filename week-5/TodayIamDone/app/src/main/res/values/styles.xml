<resources>

    <style name="LightTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorDayPrimary</item>
        <item name="colorPrimaryDark">@color/colorDayPrimaryDark</item>
        <item name="colorAccent">@color/colorDayAccent</item>

        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorPrimaryDisableOnly">@android:color/black</item>

        <item name="android:headerBackground">@color/colorDayPrimary</item>
        <item name="android:windowBackground">@color/colorDayBackground</item>

        <item name="editTextColor">@android:color/black</item>
        <item name="radioButtonStyle">@android:color/black</item>
        <item name="drawableTint">@android:color/black</item>
        <item name="buttonTint">@android:color/black</item>

        <item name="android:alertDialogTheme">@style/Theme.AppCompat.Light.Dialog</item>
    </style>

    <style name="DarkTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- TODO: DarkTheme 완성 -->
    </style>

</resources>
